import './page.css'

export default function Page() {

    return (
        <div>
            <h1>Page with CSS attached</h1>
            <p>CSS link should be nonce'd, bit it's not</p>
        </div>
    )
}
